import { Store } from "redux";
import { History } from "history";
import { AppState } from "./state/AppState";
/**
 * @typedef {Object} FlexState
 * @property {AppState} flex Flex application state
 */
export interface FlexState {
    readonly flex: AppState;
}
export interface StoreSettings {
    history: History;
}
export declare function getDefaultStore(): Store<FlexState>;
export declare function createDefaultStore(settings: StoreSettings): Store<FlexState>;
