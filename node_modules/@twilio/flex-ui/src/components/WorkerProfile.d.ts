import * as React from "react";
import { ThemeProps } from "./theme";
import { DynamicContentStore } from "@twilio/frame-ui";
import { IWorker } from "../models";
interface WorkerProfileDetail {
    name: string;
    content: string;
}
export interface WorkerProfileProps {
    worker: IWorker;
    details?: Array<WorkerProfileDetail>;
}
export declare class WorkerProfile extends React.PureComponent<WorkerProfileProps & ThemeProps> {
    static readonly friendlyName = "WorkerCanvasProfile";
    static readonly displayName = "WorkerCanvasProfile";
    static readonly Content: DynamicContentStore;
    static readonly defaultProps: Partial<WorkerProfileProps>;
    render(): JSX.Element;
}
export {};
