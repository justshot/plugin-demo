import * as React from "react";
import { DynamicContentStore } from "@twilio/frame-ui";
import { ThemeProps } from "../theme";
import { ConferenceParticipant } from "../../state/ConferencesState";
import { TaskContextProps } from "../context";
export interface ParticipantCanvasProps extends ThemeProps, TaskContextProps {
    participant: ConferenceParticipant;
    participantWidth: number;
}
export declare class ParticipantCanvas extends React.PureComponent<ParticipantCanvasProps> {
    static readonly displayName = "ParticipantCanvas";
    static readonly Content: DynamicContentStore;
    render(): JSX.Element;
}
