import * as React from "react";
import { DynamicContentStore } from "@twilio/frame-ui";
import { ITask } from "../../models";
import { TaskContextProps } from "../context";
import { ThemeProps } from "../theme";
export interface TaskCardProps extends TaskContextProps {
    icon?: string | React.ReactNode;
    firstLine?: React.ReactChild;
    secondLine?: React.ReactChild;
    onClick?: ((task: ITask) => void);
    highlighted?: boolean;
    selected?: boolean;
}
export declare class TaskCard extends React.PureComponent<TaskCardProps & ThemeProps> {
    static displayName: string;
    static readonly Content: DynamicContentStore;
    private refreshTimer;
    constructor(props: TaskCardProps);
    static readonly defaultProps: {
        highlighted: boolean;
        selected: boolean;
    };
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
    private handleClick;
    private getIconAreaStyleOverride;
    private getContentAreaStyleOverride;
}
