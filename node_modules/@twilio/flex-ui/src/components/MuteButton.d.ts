import * as React from "react";
import { ButtonProps } from "@twilio/frame-ui";
export interface MuteButtonProps extends Partial<ButtonProps> {
    muted?: boolean;
    large?: boolean;
}
export declare class MuteButton extends React.PureComponent<Partial<MuteButtonProps>, undefined> {
    render(): JSX.Element;
    private onMuteClick;
}
