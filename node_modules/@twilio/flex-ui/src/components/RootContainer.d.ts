import * as React from "react";
import { DynamicContentStore, FlexBoxProperties } from "@twilio/frame-ui";
import { ThemeProps, Theme, StyledOtherComponent } from "./theme";
import { Config } from "../state/AppConfig";
export interface RootContainerProps {
    identity?: string;
    currentLoginState?: string;
    config: Config;
}
export declare class RootContainer extends React.PureComponent<Partial<RootContainerProps> & ThemeProps> {
    private static readonly friendlyName;
    static readonly displayName = "RootContainer";
    static readonly Content: DynamicContentStore;
    render(): JSX.Element;
}
export declare const Container: StyledOtherComponent<{}, FlexBoxProperties, Theme>;
