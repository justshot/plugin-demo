import * as React from "react";
import { AppState } from "../state";
import { DynamicContentStore } from "@twilio/frame-ui";
import { CallMonitoringStatus } from "../state/SupervisorState";
import { ITask, IWorker } from "../models";
export interface LiveCommsBarProps {
    monitoredTask?: ITask;
    monitoredWorker?: IWorker;
    monitoringState?: CallMonitoringStatus;
    currentCallTask?: ITask;
    currentCallOnHold?: boolean;
    selectedView?: string;
    incomingCallTask?: ITask;
}
export declare const mapTaskStateToProps: (store: AppState) => LiveCommsBarProps;
export declare class LiveCommsBar extends React.PureComponent<LiveCommsBarProps> {
    static readonly friendlyName = "LiveCommsBar";
    static readonly displayName = "LiveCommsBar";
    static readonly Content: DynamicContentStore;
    render(): JSX.Element;
}
export declare class LiveCommsBarImpl extends React.PureComponent<LiveCommsBarProps> {
    render(): JSX.Element;
}
export declare class LiveCommsBarItem extends React.PureComponent<LiveCommsBarProps> {
    private static readonly friendlyName;
    static readonly displayName = "LiveCommsBarItem";
    static readonly Content: DynamicContentStore;
    render(): JSX.Element;
    private getMessage;
    private getActions;
}
