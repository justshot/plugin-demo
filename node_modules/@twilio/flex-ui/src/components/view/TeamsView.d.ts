import * as React from "react";
import { RouteComponentProps } from "react-router";
import { DynamicContentStore, FlexBoxProperties } from "@twilio/frame-ui";
import { IWorker } from "../../models";
import { SupervisorWorkerState } from "../../state/SupervisorState";
import { ThemeProps, Theme, StyledOtherComponent } from "../theme";
import { ViewAdditionalProps } from "../ViewCollection";
interface TeamsViewProps {
    selectedWorker?: IWorker;
    monitoredTaskSid?: string;
    workers?: Array<SupervisorWorkerState>;
    stickyWorker?: SupervisorWorkerState;
    route?: RouteComponentProps<TeamsViewRouteParams>;
}
interface TeamsViewRouteParams {
    resource?: "tasks" | "workers";
    sid?: string;
}
export declare class TeamsView extends React.PureComponent<TeamsViewProps & ThemeProps & ViewAdditionalProps> {
    static readonly displayName = "TeamsView";
    static readonly Content: DynamicContentStore;
    private getBaseRoute;
    render(): JSX.Element;
    private renderChildrenFromRoute;
}
export declare const Container: StyledOtherComponent<{}, FlexBoxProperties, Theme>;
export {};
