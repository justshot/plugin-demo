import * as TwilioChat from "twilio-chat";
import { ITask } from "./models";
export declare class ChatListener {
    static init(client: TwilioChat.Client): void;
    static joinAndInitChannelForTask(task: ITask): void | Promise<void>;
    static leaveAndUninitChatForTask(task: ITask): void | Promise<void>;
    static initChannelForTask(task: ITask): void | Promise<void>;
    private static onChannelJoined;
}
