import { AnyAction } from "redux";
import { SyncClient } from "twilio-sync";
import { IWorker, IQueue } from "../models";
export interface InsightsState {
    readonly client: SyncClient;
}
export declare function reduce(state: InsightsState, action: AnyAction): InsightsState;
export declare class Actions {
    private static dispatchAction;
    static init(client: SyncClient): void;
    static shutdown(): void;
}
export declare class InsightsEntityCache {
    private static workers;
    private static workerSearch;
    private static queues;
    private static queueSearch;
    private static queryTimer;
    static initialize(): void;
    static destroy(): void;
    static getWorker(sid: string): IWorker;
    static getQueue(sid: string): IQueue;
}
