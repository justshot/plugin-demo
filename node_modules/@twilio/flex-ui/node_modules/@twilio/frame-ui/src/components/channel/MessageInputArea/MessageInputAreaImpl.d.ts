import * as React from "react";
import { InputAreaStyleProps } from "../MessageInput/MessageInput.Components";
import * as ChannelState from "../../../state/ChannelState";
/**
 * @typedef {Object} ChatInputAreaProps
 * @extends {InputAreaStyleProps}
 * @property {string} channelSid - Unique identifier of the Channel.
 * @property {ChannelState.ChannelState} [channel] - Represents a remote channel of communication.
 * @property {string} [placeholder] - A string which is displayed when input element is empty.
 * @property {Function} [onSendMessage] - A handler when the message is sent.
 * @property {boolean} [disabled] - Represents if the input element is disabled.
 * @property {boolean} returnKeySendsMessage - Whether pressing the return key should send a message.
 * @property {number} charLimit Identifies character limit for a single message.
 * @property {number} inputTextLength Length of the string entered in input element.
 */
export interface ChatInputAreaProps extends Partial<InputAreaStyleProps> {
    channelSid: string;
    channel?: ChannelState.ChannelState;
    placeholder?: string;
    onSendMessage: () => void;
    disabled?: boolean;
    returnKeySendsMessage: boolean;
    charLimit: number;
    inputTextLength: number;
    selectionStart?: number;
}
/**
 * @classdesc This component renders the input element.
 * @alias MessageInputArea
 * @param {ChatInputAreaProps} props - Properties to be passed to configure this component.
 * @extends {React.PureComponent}
 */
export declare class MessageInputAreaImpl extends React.PureComponent<ChatInputAreaProps, undefined> {
    static readonly displayName = "MessageInputArea";
    private _inputElement;
    private _inputContainerElement;
    private baseInputScrollHeight;
    private cachedHeight?;
    render(): JSX.Element;
    private renderCharCount;
    componentDidMount(): void;
    componentDidUpdate(): void;
    private setTextAreaRef;
    private setTextAreaContainerRef;
    private handleInputKeyPress;
    private handleInputFocus;
    private handleInputChange;
    private resize;
    private adjustRowCount;
    static defaultProps: Partial<ChatInputAreaProps>;
}
