import { ThemeProps } from "../../theme";
import * as React from "react";
import { DynamicContentStore } from "../../../DynamicComponent";
/**
 * @typedef MessageCanvasTrayProps
 * @property {boolean} [showButton] - Display a ‘Start new chat’ button in the tray.
 * @property {Function} [onButtonClick] - A handler for a ‘Start new chat’ button click.
 */
export interface MessageCanvasTrayProps {
    showButton?: boolean;
    onButtonClick?: () => void;
}
export declare const displayName = "MessageCanvasTray";
export declare const contentStore: DynamicContentStore;
export declare class MessageCanvasTrayImpl extends React.PureComponent<MessageCanvasTrayProps & ThemeProps> {
    render(): JSX.Element;
}
