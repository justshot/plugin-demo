import * as React from "react";
import { DynamicContentStore } from "../../..";
import { MessageState } from "../../../state/ChannelState";
import { MessageStyle } from "../../../state/ChatConfig";
/**
 * @classdesc Props to define Inline messagessss
 * @typedef InlineMessageProps
 * @property {MessageState} message - An object which represents a single message.
 * @property {MessageStyle} [messageStyle] - Identifies how the message should be displayed.
 */
export interface InlineMessageProps {
    message: MessageState;
    messageStyle?: MessageStyle;
}
/**
 * @classdesc This is a container used to inline message
 * @param {InlineMessageProps} props - props
 * @extends {React.PureComponent}
 */
export declare class InlineMessage extends React.PureComponent<InlineMessageProps, undefined> {
    private static readonly friendlyName;
    static readonly displayName = "InlineMessage";
    static readonly Content: DynamicContentStore;
    render(): JSX.Element;
}
