import * as React from "react";
/**
 * @typedef IconProps
 * @property {string | React.ReactNode} icon
 * @property {string} [className]
 * @property {number} [sizeMultiplier]
 */
export interface IconProps {
    icon: string | React.ReactNode;
    className?: string;
    sizeMultiplier?: number;
}
/**
 * Icon
 * @param {IconProps} iconProps - props
 * @extends {React.SFC}
 */
export declare const Icon: (iconProps: IconProps) => JSX.Element;
