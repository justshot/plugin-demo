import * as React from "react";
import { ContentFragmentProps, RemoveComponentCall, RemoveComponentCallOptions } from "./DynamicComponent";
export declare class DynamicContentStore {
    private _name;
    constructor(name: string);
    readonly name: string;
    private _removeRequests;
    private registeredFragments;
    private removeRegistration;
    add(child: React.ReactChild, options?: ContentFragmentProps): () => void;
    remove(key: React.Key, options?: RemoveComponentCallOptions): () => void;
    replace(child: React.ReactChild, options?: ContentFragmentProps): () => void;
    readonly fragments: React.ReactElement<ContentFragmentProps & {
        children?: React.ReactNode;
    }>[];
    readonly removeRequests: Array<RemoveComponentCall>;
}
